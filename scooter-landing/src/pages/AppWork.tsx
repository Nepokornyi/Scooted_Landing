import styled from "styled-components"

const VectorTest = styled.div`
    position: relative;
    width: 100%;
    height: 100vh;
`

export const AppWork = () => {
    return (
        <VectorTest>AppWork</VectorTest>
    )
}